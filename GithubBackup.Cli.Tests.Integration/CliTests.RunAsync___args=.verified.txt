'' was not matched. Did you mean one of the following?
-h
-q
-l
-v

Required command was not provided.
Unrecognized command or argument ''.

Description:
  Back up GitHub repositories and migrations.

Usage:
  ghb [command] [options]

Options:
  -q, --quiet                                                            Suppress non-essential status output.
  -l, --log-file <log-file>                                              Path to the log file.
  -v, --verbosity <Critical|Debug|Error|Information|None|Trace|Warning>  Set the verbosity level of the log file. [default: Information]
  -?, -h, --help                                                         Show help and usage information
  --version                                                              Show version information

Commands:
  manual        Interactively create and download GitHub backups.
  migrate       Create a GitHub migration for the selected repositories.
  login         Authenticate with GitHub. Tokens are resolved from --token, GITHUB_BACKUP_TOKEN, or GitHub device flow.
                Device flow tokens are cached temporarily while valid.
  migrations    List GitHub migrations for the authenticated user.
  repositories  List GitHub repositories available to the authenticated user.
  download      Download GitHub migration archives.
  backup        Create a GitHub migration for the selected repositories and download it when ready.